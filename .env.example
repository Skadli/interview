# 复制为 .env 后，只需填下面 3 个值。其余配置都已在 docker-compose 里或镜像内置。
# docker compose 会自动读取本文件。
#
# 本项目对接火山引擎的【两个产品】，共 3 个凭证：

# ① 火山「语音技术」(大模型流式 ASR) —— 用 AppKey + AccessKey 一对（火山语音的鉴权规定）
VOLC_APP_KEY=
VOLC_ACCESS_KEY=

# ② 火山「方舟」(豆包大模型) —— 1 个 API Key（LLM 回答 + 简历结构化 + 公司简报共用这一个）
ARK_API_KEY=

# （可选）想免 key 先把界面/链路跑起来：把 docker-compose 里 ASR_PROVIDER、LLM_PROVIDER 改成 mock。
